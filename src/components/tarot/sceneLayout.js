/** World-space lift so the fan reads “upper center” on screen. */
export const DECK_ELEVATION_Y = 1.05

/** ~DECK_ELEVATION_Y + arc row Y (~0.05–0.11); mesh origin is card center. */
export const SPIRIT_PLANE_Y = DECK_ELEVATION_Y + 0.09
